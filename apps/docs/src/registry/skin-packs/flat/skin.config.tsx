import type { ControlUiSkin } from "@/components/control-ui/skin";

// Flat needs no slot overrides: its complete theme owns squared corners, zero shadows, instant motion, and one neutral plane.
export const skin: ControlUiSkin = { id: "flat" };
